#pragma once

#include <type_traits>
#include <concepts>


namespace Physics {
/*
    template<typename T>
    concept PhysicsClass = requires(T a, float dt) {
        { a.update(dt) -> std::same_as<void>; };
    };
*/
}


